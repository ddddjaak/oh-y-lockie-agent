{
  "$schema": "https://opencode.ai/config.json",

  // =========================================================================
  // oh-y-lockie-agent 配置
  // =========================================================================
  // 本文件只保留「用户可调项」：每个 agent 的 model 覆盖、以及 disable 开关。
  //
  // 【注意】agent 的定义（prompt / 描述 / mode / color）已收归代码
  //        src/agents/definitions.ts，不在本文件维护。这里只改 model / disable。
  //
  // 【配置层级】（优先级从高到低）:
  //   1. 项目级:  .opencode/oh-y-lockie-agent.jsonc
  //   2. 用户级:  ~/.config/opencode/oh-y-lockie-agent.jsonc
  //   3. 插件默认: config/oh-y-lockie-agent.jsonc（本文件）
  // =========================================================================

  "agent": {

    // ── 主 Agent ──────────────────────────────────────────────────

    "architect": {
      "model": "ddddjaak/mimo-v2.5"
    },

    "firmware": {
      "model": "ddddjaak/mimo-v2.5"
    },

    // ── Subagent: 设计类 ─────────────────────────────────────────

    "power-architect": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "boot-bringup-specialist": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "memory-map-specialist": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "firmware-architect": {
      "model": "ddddjaak/mimo-v2.5"
    },

    "timing-analyst": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "register-map-generator": {
      "model": "ddddjaak/mimo-v2.5"
    },

    // ── Subagent: 审查类 ─────────────────────────────────────────

    "code-reviewer": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "security-auditor": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    "system-architect": {
      "model": "ddddjaak/mimo-v2.5-pro"
    },

    // ── Subagent: 领域专家 ───────────────────────────────────────

    "fw-domain-expert": {
      "model": "ddddjaak/mimo-v2.5"
    },

    "hw-domain-expert": {
      "model": "ddddjaak/mimo-v2.5"
    },

    "compliance-reviewer": {
      "model": "ddddjaak/mimo-v2.5"
    },

    // ── Subagent: 质量保障 ───────────────────────────────────────

    "test-engineer": {
      "model": "ddddjaak/mimo-v2.5"
    },

    "verification-engineer": {
      "model": "ddddjaak/mimo-v2.5"
    },

    // ── 禁用默认 agent ───────────────────────────────────────────

    "explore": {
      "disable": true
    },
    "general": {
      "disable": true
    }
  },

  // ── MCP 服务器配置 ─────────────────────────────────────────────
  // 以下为推荐的 MCP 服务，插件会自动注入到 opencode 运行时的 mcp 配置中。
  // 如果你已在 opencode.json 中配置了同名 MCP 服务器，插件不会覆盖。

  "mcp": {
    "codegraph": {
      "command": ["codegraph", "serve", "--mcp"],
      "enabled": true,
      "type": "local"
    },
    "context7": {
      "command": ["npx", "-y", "@upstash/context7-mcp"],
      "enabled": true,
      "type": "local"
    },
    "memory": {
      "command": ["npx", "-y", "@modelcontextprotocol/server-memory"],
      "enabled": true,
      "type": "local"
    },
    "sequential-thinking": {
      "command": ["npx", "-y", "@modelcontextprotocol/server-sequential-thinking"],
      "enabled": true,
      "type": "local"
    }
  }
}
